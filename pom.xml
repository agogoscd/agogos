<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.redhat.agogos</groupId>
    <artifactId>agogos-parent</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Agogos</name>

    <parent>
        <artifactId>jboss-parent</artifactId>
        <groupId>org.jboss</groupId>
        <version>38</version>
    </parent>

    <modules>
        <module>test</module>
        <module>core</module>
        <module>cli</module>
        <module>operator</module>
        <module>webhooks</module>
        <!-- <module>relays</module> -->
    </modules>

    <properties>
        <main.basedir>.</main.basedir>

        <maven.compiler.parameters>true</maven.compiler.parameters>
        <maven.compiler.source>11</maven.compiler.source>
        <maven.compiler.target>11</maven.compiler.target>
        <maven.min.version>3.6.2</maven.min.version>
        <version.compiler.plugin>3.8.1</version.compiler.plugin>
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.group-id>io.quarkus</quarkus.platform.group-id>
        <surefire-plugin.version>3.0.0-M5</surefire-plugin.version>
        <failsafe-plugin.version>3.0.0-M5</failsafe-plugin.version>
        <cloudevents.version>2.2.0</cloudevents.version>
        <openapi-schema-validator.version>1.0.5</openapi-schema-validator.version>
        <lombok.version>1.18.16</lombok.version>
        <quarkus-tekton-client.version>0.7.0-SNAPSHOT</quarkus-tekton-client.version>
        <bouncycastle.version>1.72</bouncycastle.version>

        <quarkus.platform.version>3.0.0.CR1</quarkus.platform.version>
        <quarkus-operatorsdk.version>6.0.0.CR1</quarkus-operatorsdk.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>${quarkus.platform.artifact-id}</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.quarkiverse.operatorsdk</groupId>
                <artifactId>quarkus-operator-sdk-bom</artifactId>
                <version>${quarkus-operatorsdk.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>io.quarkus</groupId>
                    <artifactId>quarkus-maven-plugin</artifactId>
                    <version>${quarkus.platform.version}</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>generate-code</goal>
                                <goal>generate-code-tests</goal>
                                <goal>build</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>com.diffplug.spotless</groupId>
                    <artifactId>spotless-maven-plugin</artifactId>
                    <version>2.8.1</version>
                    <configuration>
                        <formats>
                            <format>
                                <includes>
                                    <includes>**/pom.xml</includes>
                                </includes>
                                <indent>
                                    <spaces>true</spaces>
                                    <spacesPerTab>4</spacesPerTab>
                                </indent>
                            </format>
                        </formats>
                        <java>
                            <indent>
                                <spaces>true</spaces>
                            </indent>
                            <includes>
                                <include>webhooks/src/main/java/**/*.java</include>
                                <include>webhooks/src/test/java/**/*.java</include>
                                <include>operator/src/main/java/**/*.java</include>
                                <include>operator/src/test/java/**/*.java</include>
                                <include>core/src/main/java/**/*.java</include>
                                <include>core/src/test/java/**/*.java</include>
                                <include>cli/src/main/java/**/*.java</include>
                                <include>cli/src/test/java/**/*.java</include>
                            </includes>
                            <removeUnusedImports />
                            <importOrder>
                                <file>${main.basedir}/spotless-importorder.properties</file>
                            </importOrder>
                            <eclipse>
                                <file>${main.basedir}/eclipse-format.xml</file>
                            </eclipse>
                        </java>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>apply</goal>
                            </goals>
                            <phase>compile</phase>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${surefire-plugin.version}</version>
                    <configuration>
                        <systemPropertyVariables>
                            <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                        </systemPropertyVariables>
                    </configuration>
                </plugin>

                <plugin>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${failsafe-plugin.version}</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>integration-test</goal>
                                <goal>verify</goal>
                            </goals>
                            <configuration>
                                <systemPropertyVariables>
                                    <native.image.path>${project.build.directory}/${project.build.finalName}-runner</native.image.path>
                                    <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                                    <maven.home>${maven.home}</maven.home>
                                </systemPropertyVariables>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
