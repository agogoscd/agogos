---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: components.agogos.redhat.com
spec:
  group: agogos.redhat.com
  names:
    kind: Component
    listKind: ComponentList
    plural: components
    singular: component
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: Component is the Schema for the components API
          properties:
            apiVersion:
              description:
                "APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
              type: string
            kind:
              description:
                "Kind is a string value representing the REST resource this
                object represents. Servers may infer this from the endpoint the client
                submits requests to. Cannot be updated. In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
              type: string
            metadata:
              type: object
            spec:
              type: object
              description: ComponentSpec defines the desired state of Component
              properties:
                pre:
                  type: array
                  description: "List of Handlers that should be executed before the build"
                  items:
                    type: object
                    properties:
                      handlerRef:
                        type: object
                        description: Handler reference
                        properties:
                          name:
                            type: string
                            description: Name of the Handler
                        required: ["name"]
                      params:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                        description: "Parameters that should be passed to the Handler implementation"
                      config:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                        description: "Additional configuration that should be passed to the Handler implementation"
                    required: ["handlerRef"]

                post:
                  type: array
                  description: "List of Handlers that should be executed after the build"
                  items:
                    type: object
                    properties:
                      handlerRef:
                        type: object
                        description: Handler reference
                        properties:
                          name:
                            type: string
                            description: Name of the Handler
                        required: ["name"]
                      params:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                        description: "Parameters that should be passed to the Handler implementation"
                      config:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                        description: "Additional configuration that should be passed to the Handler implementation"
                    required: ["handlerRef"]

                build:
                  type: object
                  properties:
                    builderRef:
                      type: object
                      description: Builder reference
                      properties:
                        name:
                          type: string
                          description: Name of the Builder
                        kind:
                          type: string
                          description: Type of the Builder
                          default:
                      required: ["name", "kind"]
                    params:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                      description: "Parameters that should be passed to the Builder implementation"
                  required: ["builderRef"]
                dependents:
                  type: object
                  description: Dependents of this Component
                  properties:
                    components:
                      type: array
                      description: List of the Components
                      items:
                        type: string
                        description: Name of the Component
                    groups:
                      type: array
                      description: List of the Groups
                      items:
                        type: string
                        description: Name of the Group
                    pipelines:
                      type: array
                      description: List of the Pipelines
                      items:
                        type: string
                        description: Name of the Pipeline
                  anyOf:
                    - required: ["components"]
                    - required: ["groups"]
                    - required: ["pipelines"]
              required:
                - "build"
            status:
              description: ComponentStatus defines the observed state of Component
              properties:
                lastUpdate:
                  format: date-time
                  type: string
                reason:
                  type: string
                status:
                  default: New
                  enum:
                    - New
                    - Initializing
                    - Ready
                    - Failed
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Status
          jsonPath: .status.status
          type: string
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
